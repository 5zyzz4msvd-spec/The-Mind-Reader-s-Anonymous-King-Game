# The Mind Reader's Anonymous King Game

《读心术者的匿名国王游戏（louisHM制作）》的 SillyTavern 前端发布仓库。

## CDN layout

- `dist/webview/st-load-inline.html`：由角色卡正则加载的前端运行包。
- `dist/webview/assets/*.webp`：四名角色的前端证件照。

角色卡使用 commit-pinned jsDelivr URL，不依赖可变的 `main` 缓存。

本仓库只存放发布资源，不包含角色卡的隐藏剧情源文件。
